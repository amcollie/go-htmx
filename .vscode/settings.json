{
    "sqltools.useNodeRuntime": true,
    "sqltools.connections": [
        {
            "previewLimit": 50,
            "driver": "SQLite",
            "name": "movies",
            "database": "${workspaceFolder:go-htmx}/movies.db"
        }
    ]
}